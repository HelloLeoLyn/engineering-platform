// CustomerLite API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { CustomerLite, CustomerLiteCreateRequest, CustomerLiteUpdateRequest, CustomerLiteQuery, CustomerLitePageResponse } from '../types/customer-lite';
import type { EntityId } from '../types/enterprise';

export const customerLiteApi = {
  page: (query: CustomerLiteQuery = {}) =>
    http.get<CustomerLitePageResponse>('/customer_lite' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<CustomerLite>('/customer_lite/' + id),
  create: (body: CustomerLiteCreateRequest) => http.post<CustomerLite>('/customer_lite', body),
  update: (id: EntityId, body: CustomerLiteUpdateRequest) => http.put<CustomerLite>('/customer_lite/' + id, body),
  disable: (id: EntityId) => http.post<CustomerLite>('/customer_lite/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
