<script setup lang="ts">
// FormDrawer (generated by engineering-platform frontend-enterprise-management asset)
// V06-WORK-003 EP UI 2.0: soft rounded drawer with header + body + stable footer.
withDefaults(
  defineProps<{
    modelValue: boolean;
    title: string;
    width?: string;
    destroyOnClose?: boolean;
  }>(),
  { width: '520px', destroyOnClose: true },
);
const emit = defineEmits<{ 'update:modelValue': [value: boolean] }>();

function onClose(): void {
  emit('update:modelValue', false);
}
</script>

<template>
  <div class="form-drawer" data-testid="form-drawer">
    <el-drawer
      :model-value="modelValue"
      :title="title"
      :size="width"
      :destroy-on-close="destroyOnClose"
      class="ep-drawer"
      data-testid="form-drawer-panel"
      @close="onClose"
    >
      <div class="form-drawer-body">
        <slot />
      </div>
    </el-drawer>
  </div>
</template>

<style scoped>
.form-drawer-body {
  display: flex;
  flex-direction: column;
  min-height: 100%;
}
:deep(.el-drawer) {
  border-radius: var(--ep-radius-xl) 0 0 var(--ep-radius-xl);
  box-shadow: var(--ep-shadow-xl);
}
</style>
