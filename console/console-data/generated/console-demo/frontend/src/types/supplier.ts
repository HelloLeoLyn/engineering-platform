// Supplier types (generated by engineering-platform Generic Module Generator)
// API Boundary ID String Contract: Snowflake IDs cross as strings (EntityId).
import type { PageResult, EntityId } from './enterprise';

export type { PageResult, EntityId };

export interface Supplier {
  id: EntityId;
  code: string;
  name: string;
  status: string;
  category: string;
  departmentId: EntityId | null;
  createdBy: EntityId | null;
  createdAt: string;
  updatedAt: string;
}

export interface SupplierCreateRequest {
  code: string;
  name: string;
  status: string;
  category: string;
}

export interface SupplierUpdateRequest {
  code: string;
  name: string;
  status: string;
  category: string;
}

export interface SupplierQuery {
  page?: number;
  size?: number;
  keyword?: string;
  status?: string;
}

export type SupplierPageResponse = PageResult<Supplier>;
