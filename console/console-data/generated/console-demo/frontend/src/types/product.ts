// Product types (generated by engineering-platform frontend-product-reference asset)
// Stable contract for the reference business module.
// API Boundary ID String Contract: Snowflake IDs cross as strings (EntityId).
import type { PageResult, EntityId } from './enterprise';

export type { PageResult, EntityId };

export interface Product {
  id: EntityId;
  code: string;
  name: string;
  status: string; // dictionary value (product_status)
  category: string | null; // dictionary value (product_category)
  departmentId: EntityId | null;
  createdBy: EntityId | null;
  createdAt: string;
  updatedAt: string;
}

export interface ProductCreateRequest {
  code: string;
  name: string;
  status: string;
  category: string | null;
}

export interface ProductUpdateRequest {
  code: string;
  name: string;
  status: string;
  category: string | null;
}

export interface ProductQuery {
  page?: number;
  size?: number;
  keyword?: string;
  status?: string;
  category?: string;
}

export type ProductPageResponse = PageResult<Product>;
