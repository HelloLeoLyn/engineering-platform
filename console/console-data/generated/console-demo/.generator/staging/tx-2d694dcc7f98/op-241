package com.acme.core;

import com.acme.core.domain.entity.Supplier;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Supplier model unit tests (generated by engineering-platform Generic Module Generator).
 */
class SupplierModelUnitTest {

    @Test
    void entityAccessorsRoundTrip() {
        Supplier e = new Supplier();
        e.setId(1L);
        e.setCode("v");
        e.setName("v");
        e.setStatus("v");
        e.setCategory("v");
        e.setDepartmentId(1L);
        e.setCreatedBy(1L);
        assertThat(e.getId()).isEqualTo(1L);
        assertThat(e.getCode()).isEqualTo("v");
        assertThat(e.getName()).isEqualTo("v");
        assertThat(e.getStatus()).isEqualTo("v");
        assertThat(e.getCategory()).isEqualTo("v");
        assertThat(e.getDepartmentId()).isEqualTo(1L);
        assertThat(e.getCreatedBy()).isEqualTo(1L);
    }
}
