-- Organization schema (generated by engineering-platform 0.1.0)
-- V003: sys_department + sys_user.department_id
-- MySQL-compatible baseline (H2 MODE=MySQL for tests).

CREATE TABLE IF NOT EXISTS sys_department (
    id         BIGINT       NOT NULL AUTO_INCREMENT,
    parent_id  BIGINT       NULL,
    code       VARCHAR(64)  NOT NULL,
    name       VARCHAR(128) NOT NULL,
    enabled    TINYINT(1)   NOT NULL DEFAULT 1,
    sort       INT          NOT NULL DEFAULT 0,
    created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    CONSTRAINT uk_sys_department_code UNIQUE (code),
    KEY idx_sys_department_parent (parent_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

ALTER TABLE sys_user ADD COLUMN department_id BIGINT NULL;
