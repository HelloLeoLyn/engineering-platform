package com.acme.core.application.supplier;

/**
 * Supplier response DTO (generated by engineering-platform Generic Module Generator).
 * Stable API payload — never exposes the persistence entity or MyBatis types.
 */
public record SupplierResponse(
        @com.fasterxml.jackson.databind.annotation.JsonSerialize(using = com.fasterxml.jackson.databind.ser.std.ToStringSerializer.class)
        Long id,
        String code,
        String name,
        String status,
        String category,
        Long departmentId,
        Long createdBy,
        java.time.LocalDateTime createdAt,
        java.time.LocalDateTime updatedAt) {

    public static SupplierResponse of(Long id, String code, String name, String status, String category, Long departmentId, Long createdBy, java.time.LocalDateTime createdAt, java.time.LocalDateTime updatedAt) {
        return new SupplierResponse(id, code, name, status, category, departmentId, createdBy, createdAt, updatedAt);
    }
}
