package com.acme.core.api.warehouselite;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.application.datascope.DataScopeResolver;
import com.acme.core.application.warehouselite.WarehouseLiteCreateRequest;
import com.acme.core.application.warehouselite.WarehouseLiteResponse;
import com.acme.core.application.warehouselite.WarehouseLiteService;
import com.acme.core.application.warehouselite.WarehouseLiteUpdateRequest;
import com.acme.core.common.core.ApiResponse;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.common.core.PlatformException;
import com.acme.core.common.core.RequestContext;
import com.acme.core.common.security.OperationLog;
import com.acme.core.common.security.RequirePermission;
import com.acme.core.common.security.SecurityErrorCodes;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/**
 * WarehouseLite CRUD API (generated by engineering-platform Generic Module Generator).
 */
@RestController
@RequestMapping("/api/warehouse_lite")
public class WarehouseLiteController {

    private final WarehouseLiteService warehouseLiteService;
    private final DataScopeResolver dataScopeResolver;

    public WarehouseLiteController(WarehouseLiteService warehouseLiteService, DataScopeResolver dataScopeResolver) {
        this.warehouseLiteService = warehouseLiteService;
        this.dataScopeResolver = dataScopeResolver;
    }

    private Long currentUserId() {
        return Long.valueOf(RequestContext.currentUserId()
                .orElseThrow(() -> new PlatformException(SecurityErrorCodes.UNAUTHENTICATED)));
    }

    private DataPermissionContext scope() {
        return dataScopeResolver.resolve(currentUserId());
    }

    @PostMapping
    @RequirePermission("warehouse_lite:item:create")
    @OperationLog(operation = "WAREHOUSE_LITE_CREATE", resourceType = "WAREHOUSE_LITE")
    public ApiResponse<WarehouseLiteResponse> create(@RequestBody WarehouseLiteCreateRequest request) {
        Long userId = currentUserId();
        Long departmentId = RequestContext.currentDepartmentId().map(Long::valueOf).orElse(null);
        return ApiResponse.ofSuccess(warehouseLiteService.create(request, userId, departmentId));
    }

    @GetMapping("/{id}")
    @RequirePermission("warehouse_lite:item:read")
    public ApiResponse<WarehouseLiteResponse> detail(@PathVariable Long id) {
        return ApiResponse.ofSuccess(warehouseLiteService.get(id, scope()));
    }

    @GetMapping
    @RequirePermission("warehouse_lite:item:read")
    public ApiResponse<PageResult<WarehouseLiteResponse>> list(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) String status) {
        return ApiResponse.ofSuccess(warehouseLiteService.pageFiltered(PageQuery.of(page, size), scope(), keyword, status));
    }

    @PutMapping("/{id}")
    @RequirePermission("warehouse_lite:item:update")
    @OperationLog(operation = "WAREHOUSE_LITE_UPDATE", resourceType = "WAREHOUSE_LITE")
    public ApiResponse<WarehouseLiteResponse> update(@PathVariable Long id, @RequestBody WarehouseLiteUpdateRequest request) {
        return ApiResponse.ofSuccess(warehouseLiteService.update(id, request, scope()));
    }

    @PostMapping("/{id}/disable")
    @RequirePermission("warehouse_lite:item:disable")
    @OperationLog(operation = "WAREHOUSE_LITE_DISABLE", resourceType = "WAREHOUSE_LITE")
    public ApiResponse<WarehouseLiteResponse> disable(@PathVariable Long id) {
        return ApiResponse.ofSuccess(warehouseLiteService.disable(id, scope()));
    }
}
