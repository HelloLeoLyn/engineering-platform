<script setup lang="ts">
// PageHeader (generated by engineering-platform frontend-permission asset)
// V06-WORK-003B EP UI 2.0 (Visual Refinement): borderless elevated header —
// hierarchy via background contrast + spacing, not a bordered rectangle.
withDefaults(
  defineProps<{
    title: string;
    description?: string;
    /** Optional eyebrow / kicker text above the title (e.g. module name). */
    eyebrow?: string;
  }>(),
  {},
);
</script>

<template>
  <div class="page-header" data-testid="page-header">
    <div class="page-header-text">
      <p v-if="eyebrow" class="page-eyebrow">{{ eyebrow }}</p>
      <h2 class="page-title">{{ title }}</h2>
      <p v-if="description" class="page-description">{{ description }}</p>
      <div v-if="$slots.subtitle" class="page-subtitle">
        <slot name="subtitle" />
      </div>
    </div>
    <div class="page-actions">
      <slot name="actions" />
    </div>
  </div>
</template>

<style scoped>
.page-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--ep-space-4);
  padding: var(--ep-space-2) 0 var(--ep-space-5);
}
.page-eyebrow {
  margin: 0 0 var(--ep-space-1);
  font-size: var(--ep-font-size-xs);
  font-weight: var(--ep-font-weight-semibold);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--ep-accent-indigo);
}
.page-title {
  margin: 0;
  font-size: var(--ep-font-size-page);
  font-weight: var(--ep-font-weight-semibold);
  color: var(--ep-color-text);
  line-height: var(--ep-line-height-tight);
}
.page-description {
  margin: var(--ep-space-2) 0 0;
  color: var(--ep-color-text-secondary);
  font-size: var(--ep-font-size-base);
}
.page-subtitle {
  margin-top: var(--ep-space-3);
}
.page-actions {
  display: flex;
  align-items: center;
  gap: var(--ep-space-2);
  flex-shrink: 0;
  padding-top: 2px;
}
</style>
