// WarehouseLite API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { WarehouseLite, WarehouseLiteCreateRequest, WarehouseLiteUpdateRequest, WarehouseLiteQuery, WarehouseLitePageResponse } from '../types/warehouse-lite';
import type { EntityId } from '../types/enterprise';

export const warehouseLiteApi = {
  page: (query: WarehouseLiteQuery = {}) =>
    http.get<WarehouseLitePageResponse>('/warehouse_lite' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<WarehouseLite>('/warehouse_lite/' + id),
  create: (body: WarehouseLiteCreateRequest) => http.post<WarehouseLite>('/warehouse_lite', body),
  update: (id: EntityId, body: WarehouseLiteUpdateRequest) => http.put<WarehouseLite>('/warehouse_lite/' + id, body),
  disable: (id: EntityId) => http.post<WarehouseLite>('/warehouse_lite/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
