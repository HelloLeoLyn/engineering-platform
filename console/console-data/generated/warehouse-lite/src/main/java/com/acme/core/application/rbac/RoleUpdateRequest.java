package com.acme.core.application.rbac;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.acme.core.common.core.LongIdDeserializer;

/**
 * Role update request (generated by engineering-platform 0.1.0).
 * code is immutable once created (stable role code governance).
 */
public record RoleUpdateRequest(
        String name,
        String dataScope,
        @JsonDeserialize(contentUsing = LongIdDeserializer.class)
        java.util.List<Long> permissionIds) {
}
