package com.acme.core.infrastructure.persistence;

import com.acme.core.application.menu.MenuPort;
import com.acme.core.domain.entity.SysMenu;
import com.acme.core.infrastructure.persistence.mapper.SysMenuMapper;
import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;

import java.util.List;

/**
 * MyBatis-Plus implementation of {@link MenuPort} (generated by engineering-platform 0.1.0).
 * All MyBatis types stay confined here, in the infrastructure layer.
 */
public class MybatisMenuRepository implements MenuPort {

    private final SysMenuMapper menuMapper;

    public MybatisMenuRepository(SysMenuMapper menuMapper) {
        this.menuMapper = menuMapper;
    }

    @Override
    public List<SysMenu> findAll() {
        return menuMapper.selectList(new QueryWrapper<SysMenu>().orderByAsc("sort", "id"));
    }
}
