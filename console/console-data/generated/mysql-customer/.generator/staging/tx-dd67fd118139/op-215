// MySqlCustomer API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { MySqlCustomer, MySqlCustomerCreateRequest, MySqlCustomerUpdateRequest, MySqlCustomerQuery, MySqlCustomerPageResponse } from '../types/mysql-customer';
import type { EntityId } from '../types/enterprise';

export const mySqlCustomerApi = {
  page: (query: MySqlCustomerQuery = {}) =>
    http.get<MySqlCustomerPageResponse>('/demo_customer' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<MySqlCustomer>('/demo_customer/' + id),
  create: (body: MySqlCustomerCreateRequest) => http.post<MySqlCustomer>('/demo_customer', body),
  update: (id: EntityId, body: MySqlCustomerUpdateRequest) => http.put<MySqlCustomer>('/demo_customer/' + id, body),
  disable: (id: EntityId) => http.post<MySqlCustomer>('/demo_customer/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
