<script setup lang="ts">
// PageContainer (generated by engineering-platform frontend-permission asset)
// V06-WORK-003B EP UI 2.0 (Visual Refinement): consistent content column
// with max-width + breathing room (4/8 rhythm between page modules).
withDefaults(defineProps<{ fullWidth?: boolean }>(), { fullWidth: false });
</script>

<template>
  <div class="page-container" :class="{ 'page-container--full': fullWidth }" data-testid="page-container">
    <slot />
  </div>
</template>

<style scoped>
.page-container {
  max-width: var(--ep-content-max-width);
  margin: 0 auto;
  padding: var(--ep-space-2) 0 var(--ep-space-6);
  display: grid;
  gap: var(--ep-space-5);
}
.page-container--full {
  max-width: none;
}
</style>
