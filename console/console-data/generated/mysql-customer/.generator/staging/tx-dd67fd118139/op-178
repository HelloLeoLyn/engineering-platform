<script setup lang="ts">
// StatusTag (generated by engineering-platform frontend-enterprise-management asset)
// V06-WORK-003 EP UI 2.0: soft pill status with accent palette.
import { computed } from 'vue';

const props = withDefaults(defineProps<{ enabled: boolean; activeText?: string; inactiveText?: string }>(), {
  activeText: 'Enabled',
  inactiveText: 'Disabled',
});

const cls = computed(() => (props.enabled ? 'is-enabled' : 'is-disabled'));
const label = computed(() => (props.enabled ? props.activeText : props.inactiveText));
</script>

<template>
  <span class="status-tag" :class="cls" data-testid="status-tag">
    <span class="status-tag-dot" />
    {{ label }}
  </span>
</template>

<style scoped>
.status-tag {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 2px 10px;
  border-radius: var(--ep-radius-pill);
  font-size: var(--ep-font-size-xs);
  font-weight: var(--ep-font-weight-medium);
  line-height: 1.6;
}
.status-tag-dot {
  width: 6px;
  height: 6px;
  border-radius: var(--ep-radius-pill);
}
.status-tag.is-enabled {
  background: var(--ep-accent-emerald-soft);
  color: var(--ep-accent-emerald);
}
.status-tag.is-enabled .status-tag-dot {
  background: var(--ep-accent-emerald);
}
.status-tag.is-disabled {
  background: var(--ep-color-info-soft);
  color: var(--ep-color-text-secondary);
}
.status-tag.is-disabled .status-tag-dot {
  background: var(--ep-color-text-muted);
}
</style>
