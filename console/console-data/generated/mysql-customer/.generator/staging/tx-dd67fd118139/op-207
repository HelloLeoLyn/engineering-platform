package com.acme.core.infrastructure.persistence;

import com.acme.core.application.mysqlcustomer.MySqlCustomerPort;
import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.application.organization.DepartmentPort;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.common.security.DataScope;
import com.acme.core.domain.entity.MySqlCustomer;
import com.acme.core.infrastructure.persistence.mapper.MySqlCustomerMapper;
import com.baomidou.mybatisplus.core.conditions.query.QueryWrapper;

import java.util.List;
import java.util.Optional;
import java.util.Set;

/**
 * MyBatis-Plus implementation of {@link MySqlCustomerPort} (generated by engineering-platform Generic Module Generator).
 * Data scope enforcement happens HERE at the query boundary: ALL / DEPARTMENT /
 * DEPARTMENT_AND_CHILDREN / SELF. Fail-safe: missing department with a department-
 * dependent scope denies (empty), never upgrades to ALL.
 */
public class MybatisMySqlCustomerRepository implements MySqlCustomerPort {

    private final MySqlCustomerMapper mySqlCustomerMapper;
    private final DepartmentPort departmentPort;

    public MybatisMySqlCustomerRepository(MySqlCustomerMapper mySqlCustomerMapper, DepartmentPort departmentPort) {
        this.mySqlCustomerMapper = mySqlCustomerMapper;
        this.departmentPort = departmentPort;
    }

    @Override
    public Optional<MySqlCustomer> findById(Long id) {
        return Optional.ofNullable(mySqlCustomerMapper.selectById(id));
    }

    @Override
    public Optional<MySqlCustomer> findByIdInScope(Long id, DataPermissionContext context) {
        return findByScope(context).stream().filter(e -> e.getId().equals(id)).findFirst();
    }

    @Override
    public List<MySqlCustomer> findByScope(DataPermissionContext context) {
        QueryWrapper<MySqlCustomer> wrapper = scopeWrapper(context);
        wrapper.orderByAsc("id");
        return mySqlCustomerMapper.selectList(wrapper);
    }

    @Override
    public PageResult<MySqlCustomer> findPageByScope(DataPermissionContext context, PageQuery query) {
        QueryWrapper<MySqlCustomer> countWrapper = scopeWrapper(context);
        long total = mySqlCustomerMapper.selectCount(countWrapper);
        QueryWrapper<MySqlCustomer> pageWrapper = scopeWrapper(context);
        pageWrapper.orderByAsc("id");
        pageWrapper.last("LIMIT " + query.size() + " OFFSET " + query.offset());
        List<MySqlCustomer> items = mySqlCustomerMapper.selectList(pageWrapper);
        return PageResult.of(items, total, query.page(), query.size());
    }

    @Override
    public PageResult<MySqlCustomer> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status) {
        QueryWrapper<MySqlCustomer> countWrapper = scopeWrapper(context);
        applyFilters(countWrapper, keyword, status);
        long total = mySqlCustomerMapper.selectCount(countWrapper);
        QueryWrapper<MySqlCustomer> pageWrapper = scopeWrapper(context);
        applyFilters(pageWrapper, keyword, status);
        pageWrapper.orderByAsc("id");
        pageWrapper.last("LIMIT " + query.size() + " OFFSET " + query.offset());
        List<MySqlCustomer> items = mySqlCustomerMapper.selectList(pageWrapper);
        return PageResult.of(items, total, query.page(), query.size());
    }

    private void applyFilters(QueryWrapper<MySqlCustomer> wrapper, String keyword, String status) {
        if (keyword != null && !keyword.isBlank()) {
            wrapper.and(w -> {w.like("code", keyword) .or().like("name", keyword) .or().like("phone", keyword) .or().like("status", keyword) .or().like("level", keyword); });
        }
        if (status != null && !status.isBlank()) {
            wrapper.eq("status", status);
        }
    }

    private QueryWrapper<MySqlCustomer> scopeWrapper(DataPermissionContext context) {
        QueryWrapper<MySqlCustomer> wrapper = new QueryWrapper<>();
        DataScope scope = context.scope() == null ? DataScope.SELF : context.scope();
        switch (scope) {
            case ALL -> { }
            case DEPARTMENT -> {
                if (context.departmentId() == null) { wrapper.eq("id", -1L); } else { wrapper.eq("department_id", context.departmentId()); }
            }
            case DEPARTMENT_AND_CHILDREN -> {
                if (context.departmentId() == null) { wrapper.eq("id", -1L); } else {
                    Set<Long> ids = departmentPort.descendantIds(context.departmentId());
                    ids.add(context.departmentId());
                    wrapper.in("department_id", ids);
                }
            }
            case SELF -> {
                if (context.userId() == null) { wrapper.eq("id", -1L); } else { wrapper.eq("created_by", String.valueOf(context.userId())); }
            }
        }
        return wrapper;
    }

    @Override
    public boolean existsByUnique(String value, Long excludeId) {
        QueryWrapper<MySqlCustomer> wrapper = new QueryWrapper<>();
        wrapper.eq("code", value);
        if (excludeId != null) { wrapper.ne("id", excludeId); }
        return mySqlCustomerMapper.selectCount(wrapper) > 0;
    }

    @Override
    public MySqlCustomer insert(MySqlCustomer entity) {
        mySqlCustomerMapper.insert(entity);
        return entity;
    }

    @Override
    public int update(MySqlCustomer entity) {
        return mySqlCustomerMapper.updateById(entity);
    }
}
