package com.acme.core.application.rbac;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.acme.core.common.core.LongIdDeserializer;

/**
 * User create request (generated by engineering-platform 0.1.0).
 * Initial password: system-generated, returned once in the create response
 * (test/ops mode). Never echoed in logs or operation log.
 */
public record UserCreateRequest(
        String username,
        String initialPassword,
        @JsonDeserialize(using = LongIdDeserializer.class)
        Long departmentId,
        @JsonDeserialize(contentUsing = LongIdDeserializer.class)
        java.util.List<Long> roleIds) {
}
