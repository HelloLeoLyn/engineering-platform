package com.acme.core;

import com.acme.core.domain.entity.MySqlCustomer;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * MySqlCustomer model unit tests (generated by engineering-platform Generic Module Generator).
 */
class MySqlCustomerModelUnitTest {

    @Test
    void entityAccessorsRoundTrip() {
        MySqlCustomer e = new MySqlCustomer();
        e.setId(1L);
        e.setCode("v");
        e.setName("v");
        e.setPhone("v");
        e.setStatus("v");
        e.setLevel("v");
        e.setDepartmentId(1L);
        e.setCreatedBy(1L);
        assertThat(e.getId()).isEqualTo(1L);
        assertThat(e.getCode()).isEqualTo("v");
        assertThat(e.getName()).isEqualTo("v");
        assertThat(e.getPhone()).isEqualTo("v");
        assertThat(e.getStatus()).isEqualTo("v");
        assertThat(e.getLevel()).isEqualTo("v");
        assertThat(e.getDepartmentId()).isEqualTo(1L);
        assertThat(e.getCreatedBy()).isEqualTo(1L);
    }
}
