-- Dictionary schema (generated by engineering-platform 0.1.0)
-- V006: sys_dictionary_type + sys_dictionary_item
-- MySQL-compatible baseline (H2 MODE=MySQL for tests).

CREATE TABLE IF NOT EXISTS sys_dictionary_type (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    code        VARCHAR(64)  NOT NULL,
    name        VARCHAR(64)  NOT NULL,
    enabled     TINYINT(1)   NOT NULL DEFAULT 1,
    description VARCHAR(255) NULL,
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    CONSTRAINT uk_sys_dictionary_type_code UNIQUE (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE IF NOT EXISTS sys_dictionary_item (
    id          BIGINT       NOT NULL AUTO_INCREMENT,
    type_id     BIGINT       NOT NULL,
    `value`     VARCHAR(64)  NOT NULL,
    label       VARCHAR(64)  NOT NULL,
    enabled     TINYINT(1)   NOT NULL DEFAULT 1,
    sort        INT          NOT NULL DEFAULT 0,
    description VARCHAR(255) NULL,
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    CONSTRAINT uk_sys_dictionary_item UNIQUE (type_id, `value`),
    KEY idx_sys_dictionary_item_type (type_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
