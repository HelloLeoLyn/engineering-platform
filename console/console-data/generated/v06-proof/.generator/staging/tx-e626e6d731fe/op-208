package com.acme.core.application.customerlite;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.domain.entity.CustomerLite;

import java.util.List;
import java.util.Optional;

/**
 * CustomerLite persistence contract (generated by engineering-platform Generic Module Generator).
 * Application/business code depends only on this interface — never on MyBatis types.
 */
public interface CustomerLitePort {

    Optional<CustomerLite> findById(Long id);

    Optional<CustomerLite> findByIdInScope(Long id, DataPermissionContext context);

    List<CustomerLite> findByScope(DataPermissionContext context);

    PageResult<CustomerLite> findPageByScope(DataPermissionContext context, PageQuery query);

    PageResult<CustomerLite> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status);

    boolean existsByUnique(String value, Long excludeId);

    CustomerLite insert(CustomerLite entity);

    int update(CustomerLite entity);
}
