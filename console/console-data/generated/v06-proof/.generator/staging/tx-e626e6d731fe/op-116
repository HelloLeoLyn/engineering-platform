package com.acme.core.application.dictionary;

import com.acme.core.domain.entity.DictionaryItem;
import com.acme.core.domain.entity.DictionaryType;

import java.util.List;
import java.util.Optional;

/**
 * Dictionary management write/read contract (generated by engineering-platform 0.1.0).
 *
 * Stable application-level management contract. The implementation lives in the
 * infrastructure layer (MyBatis mappers with hand-written SQL); application/
 * business code depends only on this interface — never on MyBatis types.
 *
 * Transaction semantics belong to the application service that orchestrates
 * these operations (validation + write is one unit of work).
 */
public interface DictionaryManagementPort {

    // ---- type ----

    List<DictionaryType> findAllTypes();

    Optional<DictionaryType> findTypeById(Long typeId);

    Optional<DictionaryType> findTypeByCode(String code);

    void insertType(DictionaryType type);

    void updateType(DictionaryType type);

    // ---- item ----

    List<DictionaryItem> findItemsByTypeId(Long typeId);

    Optional<DictionaryItem> findItemById(Long itemId);

    /** Count of items with the same value under the type (uniqueness check). */
    long countItemsByTypeAndValue(Long typeId, String value);

    void insertItem(DictionaryItem item);

    void updateItem(DictionaryItem item);
}
