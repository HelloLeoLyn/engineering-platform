package com.acme.core.application.supplier;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.domain.entity.Supplier;

import java.util.List;
import java.util.Optional;

/**
 * Supplier persistence contract (generated by engineering-platform Generic Module Generator).
 * Application/business code depends only on this interface — never on MyBatis types.
 */
public interface SupplierPort {

    Optional<Supplier> findById(Long id);

    Optional<Supplier> findByIdInScope(Long id, DataPermissionContext context);

    List<Supplier> findByScope(DataPermissionContext context);

    PageResult<Supplier> findPageByScope(DataPermissionContext context, PageQuery query);

    PageResult<Supplier> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status);

    boolean existsByUnique(String value, Long excludeId);

    Supplier insert(Supplier entity);

    int update(Supplier entity);
}
