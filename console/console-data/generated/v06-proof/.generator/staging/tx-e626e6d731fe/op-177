<script setup lang="ts">
// SearchForm (generated by engineering-platform frontend-enterprise-management asset)
// V06-WORK-003B EP UI 2.0 (Visual Refinement): tinted search panel —
// secondary surface + soft shadow, clear hierarchy vs the white data card.
import { ref } from 'vue';

withDefaults(defineProps<{ more?: boolean }>(), { more: false });
const emit = defineEmits<{ search: []; reset: [] }>();

const expanded = ref(false);
</script>

<template>
  <div class="search-panel" data-testid="search-form">
    <div class="search-panel-fields">
      <slot name="default" />
      <slot v-if="expanded" name="more" />
    </div>
    <div class="search-panel-actions">
      <el-button type="primary" data-testid="search-form-submit" @click="emit('search')">Search</el-button>
      <el-button data-testid="search-form-reset" @click="emit('reset')">Reset</el-button>
      <el-button v-if="more" text @click="expanded = !expanded">
        {{ expanded ? 'Less filters' : 'More filters' }}
        <el-icon class="el-icon--right">
          <component :is="expanded ? 'ArrowUp' : 'ArrowDown'" />
        </el-icon>
      </el-button>
    </div>
  </div>
</template>

<style scoped>
.search-panel {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--ep-space-4);
  padding: var(--ep-space-4) var(--ep-space-5);
  background: var(--ep-color-surface-tinted);
  border-radius: var(--ep-radius-lg);
  box-shadow: var(--ep-shadow-xs);
}
.search-panel-fields {
  display: flex;
  flex-wrap: wrap;
  gap: var(--ep-space-3);
  flex: 1;
}
.search-panel-actions {
  display: flex;
  align-items: center;
  gap: var(--ep-space-2);
  white-space: nowrap;
}
</style>
