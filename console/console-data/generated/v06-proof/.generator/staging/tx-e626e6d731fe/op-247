<script setup lang="ts">
// Supplier Edit (generated by engineering-platform Generic Module Generator)
// EP UI 2.0 edit: white form card + consistent action area.
import { onMounted, reactive, ref } from 'vue';
import { useRoute, useRouter } from 'vue-router';
import PageHeader from '../../components/PageHeader.vue';
import PageContainer from '../../components/PageContainer.vue';
import AppForm from '../../components/AppForm.vue';
import DictionarySelect from '../../components/DictionarySelect.vue';
import { supplierApi } from '../../api/supplier';
import type { Supplier } from '../../types/supplier';
import { errorMessage, notifySuccess } from '../../utils/feedback';

const route = useRoute();
const router = useRouter();
const row = ref<Supplier | null>(null);
const loading = ref(false);
const error = ref<string | null>(null);
const notFound = ref(false);
const submitting = ref(false);
const formError = ref<string | null>(null);
const form = reactive({code: '', name: '', status: '', category: '' });

async function load(): Promise<void> {
  loading.value = true; error.value = null; notFound.value = false;
  try { const p = await supplierApi.detail(route.params.id as string); row.value = p; Object.assign(form, {code: p.code, name: p.name, status: p.status, category: p.category }); }
  catch (e) { error.value = errorMessage(e); if (e instanceof Error && 'status' in e && (e as { status: number }).status === 404) { notFound.value = true; } }
  finally { loading.value = false; }
}

async function submit(): Promise<void> {
  if (!row.value) return;
  submitting.value = true; formError.value = null;
  try { await supplierApi.update(row.value.id, {code: form.code, name: form.name, status: form.status, category: form.category }); notifySuccess('Suppliers updated'); router.push({ path: '/supplier/' + row.value.id }); }
  catch (e) { formError.value = errorMessage(e); } finally { submitting.value = false; }
}

onMounted(load);
</script>

<template>
  <PageContainer>
    <PageHeader title="Edit Suppliers" :description="row ? String(row.id) : undefined" eyebrow="Supplier">
      <template #actions><el-button data-testid="supplier-edit-cancel" @click="router.push({ path: '/supplier' })">Cancel</el-button></template>
    </PageHeader>

    <div v-if="loading" class="edit-state" data-testid="supplier-edit-loading">Loading…</div>
    <el-result v-else-if="notFound" icon="warning" title="Suppliers not found" sub-title="It may have been removed or is outside your data scope." data-testid="supplier-edit-notfound" />
    <el-result v-else-if="error" icon="error" :title="error" data-testid="supplier-edit-error" />
    <div v-else-if="row" class="edit" data-testid="supplier-edit">
      <AppForm :model="form as unknown as Record<string, unknown>" :submitting="submitting" :error="formError" submit-text="Save" two-column @submit="submit" @cancel="router.push({ path: '/supplier/' + row.id })">
        <el-form-item label="Code" :rules="[{ required: true, message: 'Code is required' }]">
          <el-input v-model="form.code" data-testid="supplier-edit-code" />
        </el-form-item>
        <el-form-item label="Name" :rules="[{ required: true, message: 'Name is required' }]">
          <el-input v-model="form.name" data-testid="supplier-edit-name" />
        </el-form-item>
        <el-form-item label="Status">
          <DictionarySelect v-model="form.status" code="supplier_status" style="width: 100%" data-testid="supplier-edit-status" />
        </el-form-item>
        <el-form-item label="Category">
          <DictionarySelect v-model="form.category" code="supplier_category" style="width: 100%" data-testid="supplier-edit-category" />
        </el-form-item>
        <div class="edit-meta"><span>Ownership is server-managed and cannot be changed here.</span></div>
      </AppForm>
    </div>
  </PageContainer>
</template>

<style scoped>
.edit-state { padding: var(--ep-space-6); color: var(--ep-color-info); }
.edit { max-width: 860px; }
.edit-meta { font-size: var(--ep-font-size-sm); color: var(--ep-color-info); padding: 0 var(--ep-space-2); }
</style>
