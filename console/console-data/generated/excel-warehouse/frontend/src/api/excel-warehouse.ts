// ExcelWarehouse API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { ExcelWarehouse, ExcelWarehouseCreateRequest, ExcelWarehouseUpdateRequest, ExcelWarehouseQuery, ExcelWarehousePageResponse } from '../types/excel-warehouse';
import type { EntityId } from '../types/enterprise';

export const excelWarehouseApi = {
  page: (query: ExcelWarehouseQuery = {}) =>
    http.get<ExcelWarehousePageResponse>('/excel_warehouse' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<ExcelWarehouse>('/excel_warehouse/' + id),
  create: (body: ExcelWarehouseCreateRequest) => http.post<ExcelWarehouse>('/excel_warehouse', body),
  update: (id: EntityId, body: ExcelWarehouseUpdateRequest) => http.put<ExcelWarehouse>('/excel_warehouse/' + id, body),
  disable: (id: EntityId) => http.post<ExcelWarehouse>('/excel_warehouse/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
