package com.acme.core;

import com.acme.core.domain.entity.ExcelWarehouse;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * ExcelWarehouse model unit tests (generated by engineering-platform Generic Module Generator).
 */
class ExcelWarehouseModelUnitTest {

    @Test
    void entityAccessorsRoundTrip() {
        ExcelWarehouse e = new ExcelWarehouse();
        e.setId(1L);
        e.setCode("v");
        e.setName("v");
        e.setAddress("v");
        e.setManager("v");
        e.setStatus("v");
        e.setDepartmentId(1L);
        e.setCreatedBy(1L);
        assertThat(e.getId()).isEqualTo(1L);
        assertThat(e.getCode()).isEqualTo("v");
        assertThat(e.getName()).isEqualTo("v");
        assertThat(e.getAddress()).isEqualTo("v");
        assertThat(e.getManager()).isEqualTo("v");
        assertThat(e.getStatus()).isEqualTo("v");
        assertThat(e.getDepartmentId()).isEqualTo(1L);
        assertThat(e.getCreatedBy()).isEqualTo(1L);
    }
}
