-- Operation log schema (generated by engineering-platform 0.1.0)
-- V007: sys_operation_log — Generated Enterprise Application Operation Log
-- MySQL-compatible baseline (H2 MODE=MySQL for tests).
--
-- SENSITIVE-DATA SAFETY GATE: this table intentionally has NO columns for
-- passwords, tokens, secrets, request/response bodies, or environment variables.

CREATE TABLE IF NOT EXISTS sys_operation_log (
    id             BIGINT       NOT NULL AUTO_INCREMENT,
    user_id        BIGINT       NULL,
    operation      VARCHAR(64)  NOT NULL,
    resource_type  VARCHAR(64)  NOT NULL,
    resource_id    VARCHAR(64)  NULL,
    result         VARCHAR(16)  NOT NULL,
    request_id     VARCHAR(64)  NULL,
    correlation_id VARCHAR(64)  NULL,
    method         VARCHAR(128) NULL,
    path           VARCHAR(256) NULL,
    occurred_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_op_log_user (user_id),
    KEY idx_op_log_operation (operation),
    KEY idx_op_log_occurred_at (occurred_at),
    KEY idx_op_log_request (request_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
