-- excel_warehouse schema (generated by engineering-platform Generic Module Generator)
-- MySQL-compatible baseline (H2 MODE=MySQL for tests).

CREATE TABLE IF NOT EXISTS excel_warehouse (
    id            BIGINT       NOT NULL AUTO_INCREMENT,
        code VARCHAR(50) NOT NULL,
        name VARCHAR(100) NOT NULL,
        address VARCHAR(200),
        manager VARCHAR(50),
        status VARCHAR(20) NOT NULL,
    department_id BIGINT       NULL,
    created_by    VARCHAR(64)  NULL,
    created_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
    ,
    UNIQUE KEY uk_excel_warehouse_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
