package com.acme.core.infrastructure.persistence;

import com.acme.core.application.excelwarehouse.ExcelWarehousePort;
import com.acme.core.application.excelwarehouse.ExcelWarehouseService;
import com.acme.core.application.organization.DepartmentPort;
import com.acme.core.application.dictionary.DictionaryPort;
import com.acme.core.infrastructure.persistence.mapper.ExcelWarehouseMapper;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * ExcelWarehouse bean wiring (generated by engineering-platform Generic Module Generator).
 */
@Configuration
public class ExcelWarehouseBeansConfig {

    @Bean
    public ExcelWarehousePort excelWarehousePort(ExcelWarehouseMapper mapper, DepartmentPort departmentPort) {
        return new MybatisExcelWarehouseRepository(mapper, departmentPort);
    }

    @Bean
    public ExcelWarehouseService excelWarehouseService(ExcelWarehousePort port, DictionaryPort dictionaryPort) {
        return new ExcelWarehouseService(port, dictionaryPort);
    }
}
