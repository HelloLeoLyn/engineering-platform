package com.acme.core.application.excelwarehouse;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.domain.entity.ExcelWarehouse;

import java.util.List;
import java.util.Optional;

/**
 * ExcelWarehouse persistence contract (generated by engineering-platform Generic Module Generator).
 * Application/business code depends only on this interface — never on MyBatis types.
 */
public interface ExcelWarehousePort {

    Optional<ExcelWarehouse> findById(Long id);

    Optional<ExcelWarehouse> findByIdInScope(Long id, DataPermissionContext context);

    List<ExcelWarehouse> findByScope(DataPermissionContext context);

    PageResult<ExcelWarehouse> findPageByScope(DataPermissionContext context, PageQuery query);

    PageResult<ExcelWarehouse> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status);

    boolean existsByUnique(String value, Long excludeId);

    ExcelWarehouse insert(ExcelWarehouse entity);

    int update(ExcelWarehouse entity);
}
