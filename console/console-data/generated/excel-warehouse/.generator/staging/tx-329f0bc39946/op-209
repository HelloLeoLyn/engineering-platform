package com.acme.core.api.excelwarehouse;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.application.datascope.DataScopeResolver;
import com.acme.core.application.excelwarehouse.ExcelWarehouseCreateRequest;
import com.acme.core.application.excelwarehouse.ExcelWarehouseResponse;
import com.acme.core.application.excelwarehouse.ExcelWarehouseService;
import com.acme.core.application.excelwarehouse.ExcelWarehouseUpdateRequest;
import com.acme.core.common.core.ApiResponse;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.common.core.PlatformException;
import com.acme.core.common.core.RequestContext;
import com.acme.core.common.security.OperationLog;
import com.acme.core.common.security.RequirePermission;
import com.acme.core.common.security.SecurityErrorCodes;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/**
 * ExcelWarehouse CRUD API (generated by engineering-platform Generic Module Generator).
 */
@RestController
@RequestMapping("/api/excel_warehouse")
public class ExcelWarehouseController {

    private final ExcelWarehouseService excelWarehouseService;
    private final DataScopeResolver dataScopeResolver;

    public ExcelWarehouseController(ExcelWarehouseService excelWarehouseService, DataScopeResolver dataScopeResolver) {
        this.excelWarehouseService = excelWarehouseService;
        this.dataScopeResolver = dataScopeResolver;
    }

    private Long currentUserId() {
        return Long.valueOf(RequestContext.currentUserId()
                .orElseThrow(() -> new PlatformException(SecurityErrorCodes.UNAUTHENTICATED)));
    }

    private DataPermissionContext scope() {
        return dataScopeResolver.resolve(currentUserId());
    }

    @PostMapping
    @RequirePermission("excel_warehouse:item:create")
    @OperationLog(operation = "EXCEL_WAREHOUSE_CREATE", resourceType = "EXCEL_WAREHOUSE")
    public ApiResponse<ExcelWarehouseResponse> create(@RequestBody ExcelWarehouseCreateRequest request) {
        Long userId = currentUserId();
        Long departmentId = RequestContext.currentDepartmentId().map(Long::valueOf).orElse(null);
        return ApiResponse.ofSuccess(excelWarehouseService.create(request, userId, departmentId));
    }

    @GetMapping("/{id}")
    @RequirePermission("excel_warehouse:item:read")
    public ApiResponse<ExcelWarehouseResponse> detail(@PathVariable Long id) {
        return ApiResponse.ofSuccess(excelWarehouseService.get(id, scope()));
    }

    @GetMapping
    @RequirePermission("excel_warehouse:item:read")
    public ApiResponse<PageResult<ExcelWarehouseResponse>> list(
            @RequestParam(defaultValue = "1") int page,
            @RequestParam(defaultValue = "20") int size,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) String status) {
        return ApiResponse.ofSuccess(excelWarehouseService.pageFiltered(PageQuery.of(page, size), scope(), keyword, status));
    }

    @PutMapping("/{id}")
    @RequirePermission("excel_warehouse:item:update")
    @OperationLog(operation = "EXCEL_WAREHOUSE_UPDATE", resourceType = "EXCEL_WAREHOUSE")
    public ApiResponse<ExcelWarehouseResponse> update(@PathVariable Long id, @RequestBody ExcelWarehouseUpdateRequest request) {
        return ApiResponse.ofSuccess(excelWarehouseService.update(id, request, scope()));
    }

    @PostMapping("/{id}/disable")
    @RequirePermission("excel_warehouse:item:disable")
    @OperationLog(operation = "EXCEL_WAREHOUSE_DISABLE", resourceType = "EXCEL_WAREHOUSE")
    public ApiResponse<ExcelWarehouseResponse> disable(@PathVariable Long id) {
        return ApiResponse.ofSuccess(excelWarehouseService.disable(id, scope()));
    }
}
