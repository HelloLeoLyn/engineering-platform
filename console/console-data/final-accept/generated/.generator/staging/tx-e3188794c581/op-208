package com.acme.core.application.customer;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.domain.entity.Customer;

import java.util.List;
import java.util.Optional;

/**
 * Customer persistence contract (generated by engineering-platform Generic Module Generator).
 * Application/business code depends only on this interface — never on MyBatis types.
 */
public interface CustomerPort {

    Optional<Customer> findById(Long id);

    Optional<Customer> findByIdInScope(Long id, DataPermissionContext context);

    List<Customer> findByScope(DataPermissionContext context);

    PageResult<Customer> findPageByScope(DataPermissionContext context, PageQuery query);

    PageResult<Customer> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status, String code, String name);

    boolean existsByUnique(String value, Long excludeId);

    Customer insert(Customer entity);

    int update(Customer entity);
}
