package com.acme.core.application.warehouse;

import com.acme.core.application.datascope.DataPermissionContext;
import com.acme.core.common.core.PageQuery;
import com.acme.core.common.core.PageResult;
import com.acme.core.domain.entity.Warehouse;

import java.util.List;
import java.util.Optional;

/**
 * Warehouse persistence contract (generated by engineering-platform Generic Module Generator).
 * Application/business code depends only on this interface — never on MyBatis types.
 */
public interface WarehousePort {

    Optional<Warehouse> findById(Long id);

    Optional<Warehouse> findByIdInScope(Long id, DataPermissionContext context);

    List<Warehouse> findByScope(DataPermissionContext context);

    PageResult<Warehouse> findPageByScope(DataPermissionContext context, PageQuery query);

    PageResult<Warehouse> findPageByScopeFiltered(DataPermissionContext context, PageQuery query, String keyword, String status, String code, String name);

    boolean existsByUnique(String value, Long excludeId);

    Warehouse insert(Warehouse entity);

    int update(Warehouse entity);
}
