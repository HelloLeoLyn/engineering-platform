package com.acme.core.infrastructure.persistence;

import com.acme.core.application.customer.CustomerPort;
import com.acme.core.application.customer.CustomerService;
import com.acme.core.application.organization.DepartmentPort;
import com.acme.core.application.dictionary.DictionaryPort;
import com.acme.core.infrastructure.persistence.mapper.CustomerMapper;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Customer bean wiring (generated by engineering-platform Generic Module Generator).
 */
@Configuration
public class CustomerBeansConfig {

    @Bean
    public CustomerPort customerPort(CustomerMapper mapper, DepartmentPort departmentPort) {
        return new MybatisCustomerRepository(mapper, departmentPort);
    }

    @Bean
    public CustomerService customerService(CustomerPort port, DictionaryPort dictionaryPort) {
        return new CustomerService(port, dictionaryPort);
    }
}
