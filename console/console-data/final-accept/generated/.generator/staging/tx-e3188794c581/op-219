// Customer API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { Customer, CustomerCreateRequest, CustomerUpdateRequest, CustomerQuery, CustomerPageResponse } from '../types/customer';
import type { EntityId } from '../types/enterprise';

export const customerApi = {
  page: (query: CustomerQuery = {}) =>
    http.get<CustomerPageResponse>('/customer' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<Customer>('/customer/' + id),
  create: (body: CustomerCreateRequest) => http.post<Customer>('/customer', body),
  update: (id: EntityId, body: CustomerUpdateRequest) => http.put<Customer>('/customer/' + id, body),
  disable: (id: EntityId) => http.post<Customer>('/customer/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
