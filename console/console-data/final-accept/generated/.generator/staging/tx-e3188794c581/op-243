// Warehouse API service (generated by engineering-platform Generic Module Generator)
// All HTTP through the Platform Request Client — no raw axios/fetch.
import { http } from './request';
import type { Warehouse, WarehouseCreateRequest, WarehouseUpdateRequest, WarehouseQuery, WarehousePageResponse } from '../types/warehouse';
import type { EntityId } from '../types/enterprise';

export const warehouseApi = {
  page: (query: WarehouseQuery = {}) =>
    http.get<WarehousePageResponse>('/warehouse' + queryString(query as unknown as Record<string, unknown>)),
  detail: (id: EntityId) => http.get<Warehouse>('/warehouse/' + id),
  create: (body: WarehouseCreateRequest) => http.post<Warehouse>('/warehouse', body),
  update: (id: EntityId, body: WarehouseUpdateRequest) => http.put<Warehouse>('/warehouse/' + id, body),
  disable: (id: EntityId) => http.post<Warehouse>('/warehouse/' + id + '/disable', {}),
};

function queryString(params: Record<string, unknown>): string {
  const entries = Object.entries(params).filter(([, v]) => v !== undefined && v !== null && v !== '');
  if (entries.length === 0) return '';
  return '?' + entries.map(([k, v]) => encodeURIComponent(k) + '=' + encodeURIComponent(String(v))).join('&');
}
