// Auth API (generated by engineering-platform frontend-auth asset)
import { http } from './request';
import type { CurrentUser, LoginRequest, LoginResponse } from '../types/auth';

export const authApi = {
  login: (payload: LoginRequest) => http.post<LoginResponse>('/auth/login', payload),
  me: () => http.get<CurrentUser>('/users/me'),
};
