// Customer types (generated by engineering-platform Generic Module Generator)
// API Boundary ID String Contract: Snowflake IDs cross as strings (EntityId).
import type { PageResult, EntityId } from './enterprise';

export type { PageResult, EntityId };

export interface Customer {
  id: EntityId;
  code: string;
  name: string;
  phone: string;
  status: string;
  level: string;
  departmentId: EntityId | null;
  createdBy: EntityId | null;
  createdAt: string;
  updatedAt: string;
}

export interface CustomerCreateRequest {
  code: string;
  name: string;
  phone: string;
  status: string;
  level: string;
}

export interface CustomerUpdateRequest {
  code: string;
  name: string;
  phone: string;
  status: string;
  level: string;
}

export interface CustomerQuery {
  page?: number;
  size?: number;
  keyword?: string;
  status?: string;
  code?: string;
  name?: string;
}

export type CustomerPageResponse = PageResult<Customer>;
