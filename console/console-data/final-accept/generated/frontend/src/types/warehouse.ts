// Warehouse types (generated by engineering-platform Generic Module Generator)
// API Boundary ID String Contract: Snowflake IDs cross as strings (EntityId).
import type { PageResult, EntityId } from './enterprise';

export type { PageResult, EntityId };

export interface Warehouse {
  id: EntityId;
  code: string;
  name: string;
  address: string;
  manager: string;
  status: string;
  departmentId: EntityId | null;
  createdBy: EntityId | null;
  createdAt: string;
  updatedAt: string;
}

export interface WarehouseCreateRequest {
  code: string;
  name: string;
  address: string;
  manager: string;
  status: string;
}

export interface WarehouseUpdateRequest {
  code: string;
  name: string;
  address: string;
  manager: string;
  status: string;
}

export interface WarehouseQuery {
  page?: number;
  size?: number;
  keyword?: string;
  code?: string;
  name?: string;
}

export type WarehousePageResponse = PageResult<Warehouse>;
