package com.acme.core.application.rbac;

import com.acme.core.domain.entity.SysPermission;

import java.util.List;
import java.util.Optional;

/**
 * Permission governance read contract (generated by engineering-platform 0.1.0).
 *
 * READ-ONLY registry-style management: permission codes come from system/asset
 * declarations, not from UI creation. No create/delete operations exist.
 * Implementation lives in the infrastructure layer; application code depends
 * only on this interface — never on MyBatis persistence types.
 */
public interface PermissionManagementPort {

    /** Permissions optionally filtered by code (like match), ordered by code. */
    List<SysPermission> list(String code);

    Optional<SysPermission> findById(Long permissionId);
}
