# mybatis-plus — Provider Asset

MyBatis-Plus provider（persistence capability 的技术实现）。

- implements/provides: persistence（compatibility.requiredCapabilities: [persistence]）
- 兼容: Java 25 / Spring Boot 3.x reference stack
- Maven dependency: com.baomidou:mybatis-plus-spring-boot3-starter:3.5.17
  （Maven dependency 只是 Provider 实现的一部分——Provider ≠ Maven Dependency）
- files: application-mybatis.yaml / MapperScan.java（render 模板）
