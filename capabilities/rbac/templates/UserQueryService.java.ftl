package ${package}.application.rbac;

import ${package}.domain.entity.SysUser;

import java.util.List;
import java.util.Optional;
import java.util.Set;

/**
 * RBAC query application service (generated by engineering-platform ${asset.version}).
 *
 * Depends only on the stable {@link UserQueryPort} contract — the MyBatis-Plus
 * implementation lives in the infrastructure layer. Application/business code
 * never sees MyBatis persistence types.
 */
public class UserQueryService {

    private final UserQueryPort queryPort;

    public UserQueryService(UserQueryPort queryPort) {
        this.queryPort = queryPort;
    }

    public Optional<SysUser> findByUsername(String username) {
        return queryPort.findByUsername(username);
    }

    public Optional<SysUser> findById(Long userId) {
        return queryPort.findById(userId);
    }

    public List<SysUser> findAll() {
        return queryPort.findAll();
    }

    /** All permission codes granted to a user via their enabled roles. */
    public Set<String> permissionCodesOf(Long userId) {
        return queryPort.permissionCodesOf(userId);
    }
}
