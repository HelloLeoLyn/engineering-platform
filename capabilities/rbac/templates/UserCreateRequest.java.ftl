package ${package}.application.rbac;

/**
 * User create request (generated by engineering-platform ${asset.version}).
 * Initial password: system-generated, returned once in the create response
 * (test/ops mode). Never echoed in logs or operation log.
 */
public record UserCreateRequest(
        String username,
        String initialPassword,
        Long departmentId,
        java.util.List<Long> roleIds) {
}
