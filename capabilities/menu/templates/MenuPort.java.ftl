package ${package}.application.menu;

import ${package}.domain.entity.SysMenu;

import java.util.List;

/**
 * Menu read contract (generated by engineering-platform ${asset.version}).
 *
 * Stable application-level query contract. The implementation lives in the
 * infrastructure layer (MyBatis-Plus); application/business code depends only
 * on this interface — never on MyBatis persistence types.
 */
public interface MenuPort {

    /** All menus ordered by sort then id (stable tree input). */
    List<SysMenu> findAll();
}
