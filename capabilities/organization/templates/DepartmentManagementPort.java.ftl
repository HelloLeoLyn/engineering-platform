package ${package}.application.organization;

import ${package}.domain.entity.SysDepartment;

import java.util.List;
import java.util.Optional;

/**
 * Department management write/read contract (generated by engineering-platform ${asset.version}).
 *
 * Stable application-level management contract. The implementation lives in the
 * infrastructure layer (MyBatis-Plus); application/business code depends only
 * on this interface — never on MyBatis persistence types.
 *
 * Transaction semantics belong to the application service that orchestrates
 * these operations (structural validation + write is one unit of work).
 */
public interface DepartmentManagementPort {

    Optional<SysDepartment> findById(Long departmentId);

    List<SysDepartment> findAll();

    /** True when a department with the code exists. */
    boolean codeExists(String code);

    void insert(SysDepartment department);

    void update(SysDepartment department);
}
