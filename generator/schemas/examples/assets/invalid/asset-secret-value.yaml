schemaVersion: 1
id: secret-leak
type: CAPABILITY
version: 0.1.0
description: Configuration carries a plaintext secret value (forbidden).
configuration:
  - key: db.password
    type: string
    required: true
    value: "hunter2"
    description: Plaintext secret must never appear in contract.
